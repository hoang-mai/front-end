'use client';
import React from 'react';

const LoaderSelect: React.FC = () => {
    return (
        <div className={`w-20 animate-pulse h-10 bg-gray-300 rounded`}></div>
    );
};

export default LoaderSelect;
